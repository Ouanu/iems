# 应用名
spring.application.name=iems

# 默认 profile 可由环境变量覆盖（.env.dev/.env.test/.env.prod 已定义 SPRING_PROFILES_ACTIVE）
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# 服务端口（可由环境覆盖）
server.port=${SERVER_PORT:5770}
